import {colors} from './colors';

export const theme = {
  colors,
  font: {
    thin: 300,
    regular: 400,
    bold: 800,
  },
  media: {
    tablet: '@media (min-width: 728px)',
    desktop: '@media (min-width: 1024px)',
  }
}